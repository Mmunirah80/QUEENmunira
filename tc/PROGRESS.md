## Development Progress

This file summarizes ongoing autonomous work on the Naham cook/customer app.

- **Milestone 1 – Auth flow**: Unified Login screen for all users, Role Selection only for registration, cook/customer signup flows wired to Supabase `profiles` and `chef_profiles` with pending/approved/rejected states, splash/onboarding/routes updated to always enter via Login (completed earlier in this session).

- **Milestone 2 – Cook Orders Screen**: Cook `OrdersScreen` now uses Supabase realtime `orders`/`order_items` via `OrdersFirebaseDataSource`. New tab shows customer, items, total, and a 5-minute countdown timer per order; auto-rejects at 0:00 with reason "Time expired" (status `cancelled_by_cook`). Reject opens a reason picker (Busy / Item sold out / Closed / Other) and saves `rejection_reason` to Supabase. Active tab shows inline "Chat" + status buttons ("Start Preparing" for accepted → `preparing`, "Mark Ready" for preparing → `ready`). Completed and Cancelled tabs list customer, items, total, date; Cancelled shows rejection reason. All four tabs update in realtime via Supabase streams.

- **Milestone 8 – Final Polish (Cook app)**: All cook-facing screens now use consistent loading indicators, empty states, and friendly error messages backed by `userFriendlyErrorMessage`, with English-only UI copy and "Cook" terminology. Bottom navigation between Home, Orders, Menu, Reels, Chat, and Profile is wired via `ChefShell` and `RouteNames.chef*` routes, and logout from profile/settings returns cleanly to the unified Login screen without flicker. Realtime behaviour for orders and chat is verified end-to-end (new orders, status transitions, auto-reject timer, and chat messages) and a full self-check confirms cook login flows, menu/AI pricing, profile editing, documents upload (Freelancer ID + National ID), reels (including 2-minute max duration and likes), Earnings & Insights monthly cards (Supabase-backed), and logout all work together without crashes.
